<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Respuestas</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
        crossorigin="anonymous"></script>
</head>

<body>
    <body>
        <h1 id="totalCorrectas">Tuviste: </h1>
        <ol>
        <% @preguntas.each do |pregunta| %>
        <h3>Pregunta: <%= pregunta['Pregunta']%></h3>
        <br>
            <div class="card">
          <div class="card-body">
            <h5>Respuesta correcta: <%= pregunta['Correcta']%></h5>
          </div>
        </div>
        <br>
        <div class="card">
          <div class="card-body">
            <h5>Tu respuesta: <%= @resp[@n]%></h5>
          </div>
        </div>
        <br>
            <%@n = @n + 1%>
        <%end%>
        </ol>
        <a id="boton" class="btn btn-primary" type="button">Regresar a la principal</a>
        <script>
            let totalCorrectas = 0;
            let respuestaTemp = "";
            let n = <%= @cantidadPreguntas%>
            let preguntas = <%= @preguntas.to_json %>;
            let resp = <%=@resp%>
            for (let i = 0; i < n; i++) {
                respuestaTemp = resp[i]
                if (respuestaTemp == preguntas[i].Correcta) {
                    totalCorrectas++;
                }
            }
            console.log("Total correctas" + totalCorrectas)

            document.getElementById("totalCorrectas").innerHTML += totalCorrectas + " de " + n;
            document.getElementById("boton").onclick = function() {myFunction()};
            function myFunction() {
                location.href = "/";
            }
        </script>
    </body>
</body>

</html>